# What this changes

<!-- One or two sentences. If it fixes an issue, write: Fixes #123 -->

## Why

<!-- What was wrong or missing. If this changes behaviour that was measured (timings,
     quality, thresholds), say what you measured and on what material. -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes, and there is a test that fails without this change
- [ ] Tests still need no ffmpeg, no network and no API keys
- [ ] Both READMEs updated if user-visible behaviour changed
- [ ] No third-party binaries or model weights added to the repository
