import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    include: ['tests/**/*.test.ts'],
    environment: 'node',
    /*
     * Пять секунд по умолчанию — мало для этого набора.
     *
     * Часть проверок поднимает настоящий локальный сервер и дёргает ручки,
     * которые опрашивают систему: ищут программы на диске, запускают Python.
     * На холодном раннере CI это иногда не укладывалось в умолчание, и три
     * проверки краснели на ровном месте. Пятнадцать секунд по-прежнему ловят
     * зависание, но не наказывают за медленную машину.
     */
    testTimeout: 15_000,
  },
});
